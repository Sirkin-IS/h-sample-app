apply plugin: "war"

bootWar {
    mainClassName = "ru.crpt.edo.roaming.SampleApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
